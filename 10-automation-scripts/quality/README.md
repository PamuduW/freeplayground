# Quality tooling helpers
This folder stores custom helper scripts for linting and formatting.

## Files
- `strip_md_heading_blank_lines.py`: removes blank lines directly after Markdown headings.

## Why this folder exists
I keep custom quality logic in one place while still keeping standard config files in repo root for tool discovery.
